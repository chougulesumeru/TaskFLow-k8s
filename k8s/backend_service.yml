# configure backend service file

apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: todo-app
spec:
  selector:
    app: backend-deployment   # fixed: was "app: backend" — must match pod label in backend_deployment.yml
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
